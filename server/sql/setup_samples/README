The format of the setup files is:

  setup_XX_description.sql

being XX the priority of the file: a file called setup_01_foo will be run before a file called setup_02_foo.

The priorities at the moment are:

  01 -> Functions
  02 -> Administrators and professors
  03 -> Students and External Entities
  04 -> Uncommon authentication systems
  05 -> Groups
  06 -> Categories
  07 -> Experiments

